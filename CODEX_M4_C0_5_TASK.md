# CODEX M4 C0.5 — Real GADEN Intervention Gate

Switch to:

`research/causal-compositional-plume-world-model-v1`

Read first:

`evidence/causal_compositional_plume_world_model_v1/C0_5_REAL_GADEN_INTERVENTION_BANK_CHARTER_20260923.md`

Then execute strictly in order.

Priority:
1. audit same-House physical wind interventions;
2. benchmark one new source generation at 30/120/300 s;
3. if valid, generate exactly the 2 source × 2 wind × 2 independent-seed bank;
4. export spatial plume slices;
5. run the preregistered held-out recombination comparison:
   - matched-capacity monolithic model;
   - operator-compositional model `Q_s = I_S(S); C = T_{W,O}(Q_s)`;
6. freeze models, then evaluate PMFS truth-containing candidate rank;
7. run destructive nulls;
8. write the final decision.

Important:
- additive source+wind embedding/factorization is already rejected;
- do not use arbitrary wind rotation as a physical intervention;
- do not use truth to choose source, wind, duration, architecture, or hyperparameters;
- do not touch live PMFS/ROS closed loop yet;
- if no second physical same-geometry wind exists, stop with HOLD instead of inventing one;
- commit/push every stage immediately.
