# AUX PRO NEXT TASK — Red-team IF-CV V1 Before D1R Inspection

Date: 2026-09-25

The primary thread has accepted your core corrections and frozen a new theory branch.

Current primary candidate: IF-CV information-fidelity-constrained connected pooling with same-task microcell proper-score validation.

Do not reopen Track A/B competition and do not run experiments.

## 1. Attack the fidelity-certificate sample complexity

Primary pre-data tolerances are currently:
- epsilon_enc = 0.15 average-TV units;
- epsilon_mark = 0.20 average-TV units.

Using only theory and the registered N=168, R=16, d=300 design:
- determine whether the proposed simultaneous CP/DKW construction has any realistic chance of certifying a nontrivial group;
- derive sample-complexity scaling for a non-vacuous certificate;
- distinguish worst-case distribution-free impossibility from model-assisted possibilities;
- do not use D1R outcomes.

If R=16 is mathematically too weak for the strong certificate, say so clearly. Do not recommend relaxing epsilon after seeing data.

## 2. Audit the IF-CV candidate versus ordinary methods

Primary branch now treats the old DeltaF < Q/(2nN) merge rule as a baseline.

Audit whether IF-CV, as now defined, is operationally distinct from:
- agglomerative information bottleneck;
- robust profile clustering;
- MDL/prequential pooling;
- empirical hierarchical shrinkage;
- Bayesian tree pooling;
- posterior projection.

State the minimum mechanism evidence needed before the stronger causal-emergence-inspired interpretation remains justified.

## 3. Audit fixed-panel D1C statistics

Primary precommit currently uses:
- delta_id = log2(1.15) ~= 0.2016 bits/target;
- delta_ordinary = log2(1.05) ~= 0.0704 bits/target;
- one-sided alpha=0.025;
- eta_cal=0.05;
- eta_abs=0.10;
- J selected from {2,4,8,16};
- planning alternatives log2(1.30) vs identity and log2(1.10) vs ordinary.

Check whether these margins are statistically coherent and scientifically interpretable before D1R is inspected.

Do not replace them with numbers tuned to expected D1R performance.

Audit the proposed fixed-panel source-stratified within-source bootstrap-t, especially for J=2/4 and heavy-tailed paired log scores.

## 4. Real-flight / bank-dependence audit

The user explicitly does not want an algorithm that requires rebuilding a 168x16 bank in every real environment.

Derive a realistic transfer ladder:
- what is learned once in simulation;
- what may need re-learning per geometry/wind regime;
- what minimal real calibration could be acceptable;
- what must be online-computable from UAV observations;
- what evidence would show the scale-selection principle transfers without a full source bank.

Give an explicit STOP condition if deployment inherently requires exhaustive per-source repeated releases.

## 5. Deliver only

1. IFCV_CERTIFICATE_POWER_AUDIT.md
2. IFCV_VS_ORDINARY_NOVELTY_AUDIT.md
3. D1C_PRECOMMIT_REDTEAM.md
4. SIM_TO_REAL_BANK_DEPENDENCE.md
5. one-page recommendation: keep / downgrade / stop strong causal-emergence-inspired claim before D1R is opened.

Do not run D1R.
Do not generate targets.
Do not modify Codex execution branch.
Do not choose thresholds from unpublished D1R results.