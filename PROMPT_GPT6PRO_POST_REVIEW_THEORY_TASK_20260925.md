# Auxiliary Pro — Post-Review Narrow Assignment

Date: 2026-09-25

The primary thread has digested your four memos and revised the mainline.

Your red-team correction is accepted:

- a fixed source label is not automatically a dynamical microstate;
- deterministic coarse-graining under the same fixed prior cannot create
  Shannon information;
- uniform-macro EI can change the intervention distribution;
- the decisive endpoint must return to the same microcell support/prior using
  fresh-target proper scoring.

The primary thread has therefore **withdrawn the previous D1A scientific gate**.

Codex is now authorized only to build a 168-source ×16-realization reference
bank. No final targets will be generated yet.

## Your next task is narrower than before

Do not search for a third route.

Do not redesign R0.

Do not choose experimental seeds.

Solve the following theory problem for Track B.

---

## Task 1 — Decide the strongest legitimate theory name

Answer with primary-source support:

Can a static controlled intervention channel

[
S \to Y, qquad P(Y|do(S=s))
]

with a fixed unknown source parameter, be legitimately placed inside modern
causal-emergence / CE 2.0 theory **without** pretending that S itself is a
physical Markov state and without obtaining the effect only by changing the
intervention prior?

Distinguish carefully:

1. classical temporal/Markov causal emergence;
2. causal models / intervention channels that are not temporal dynamics;
3. CE 2.0 / multiscale causal contribution;
4. ordinary statistical coarse-graining / rate-distortion / MDL;
5. Blackwell / Le Cam statistical-experiment equivalence or deficiency, if
   relevant.

Return one of:

- stronger causal-emergence framing is mathematically defensible;
- causal emergence should remain mother-theory inspiration only;
- another far-domain theory is actually the mathematically correct parent.

Do not preserve the phrase "causal emergence" for rhetorical reasons.

---

## Task 2 — Derive a fixed-prior same-task objective

The final prediction support remains all N micro source cells.

The prior (pi_s) is identical for every compared model.

A hard partition (g) may share statistics but must output a microcell
posterior via honest lifting.

Derive a clean decomposition of fresh microcell log risk into, if possible:

- irreducible Bayes/source uncertainty;
- approximation/fidelity loss caused by grouping;
- finite-sample estimation/generalization error;
- model/partition complexity.

Start from your B9/B10/B14/B16/B22/B24 derivations and make the result precise.

The desired scientific interpretation is:

> a macro representation is useful only when finite-sample estimation-risk
> reduction exceeds the information/fidelity lost by grouping.

If this can be written as a theorem/proposition under explicit assumptions,
do so and state every assumption.

---

## Task 3 — Define the partition object before targets

We need a reference-only algorithm that is not just geometry-only Ward and is
not allowed to inspect final targets.

Derive one auditable candidate based on the source-observation channel.

Possible ingredients to assess:

- B17 marginal-query channel;
- weighted Bernoulli KL / Jensen-Shannon fidelity loss;
- graph-constrained adjacent merges;
- prequential microcell log loss;
- MDL / complexity penalty;
- co-membership uncertainty across reference halves.

You are not required to use all of them.

The algorithm must specify:

1. what quantity determines whether two adjacent microcells may share a
   macrostate;
2. how K / scale is selected using only reference data;
3. how the final partition is frozen;
4. how uncertainty / unstable memberships are handled;
5. how to prevent all-in-one or ordinary geometric smoothing from winning
   trivially.

---

## Task 4 — Strong baselines and equivalence audit

For the exact same final microcell proper-score task, define the minimum
baselines required to show that the proposed object is not merely:

- random pooling;
- geometry-only coarse grid;
- ordinary profile clustering;
- hierarchical Bayesian shrinkage / partial pooling;
- generic MDL;
- information bottleneck.

If one of these is mathematically equivalent to the proposed method under our
assumptions, say so explicitly. That would be a valuable NO-GO for novelty.

---

## Task 5 — Pre-registration quantities, but not arbitrary old-result tuning

The primary thread will choose final numerical thresholds.

You should derive natural scale-free quantities and recommend how to choose
thresholds *before final targets*, including:

- minimum proper-score gain (delta_B) in bits/source/target;
- partition stability (ARI/VI/co-membership);
- within-group fidelity-loss bound;
- calibration non-inferiority;
- probability-mass / credible-region diagnostics.

Do not choose a number because old failures happened to sit below it.

If a natural decision-theoretic interpretation exists, provide it.

---

## Task 6 — Track A only as a possible auxiliary

Do not promote Track A.

Explain only one question:

If Track B survives, can the finite-history source likelihood from Track A be
used later as an **effect/observation channel enrichment** without changing the
macrostate theory?

Current data permit only 10 snapshot histories; no continuous whiff/hazard
claim.

State what extra evidence would be required before this becomes auxiliary
innovation 1.

---

## Deliver exactly

1. `B_THEORY_LEGITIMACY_DECISION.md`
2. `B_FIXED_PRIOR_RISK_DECOMPOSITION.md`
3. `B_REFERENCE_ONLY_PARTITION_ALGORITHM.md`
4. `B_BASELINE_EQUIVALENCE_REDTEAM.md`
5. `A_AS_AUXILIARY_ONLY.md`

At the top include a five-line executive answer:

- Can we still call the main idea causal emergence?
- What is the precise mathematical object?
- What is the one decisive novelty risk?
- What must D1C beat?
- What result would make you advise the primary thread to STOP B entirely?

Do not authorize experiments.
Do not create a third mainline.
