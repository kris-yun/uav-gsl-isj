# Source this only in the M1R arm's own launch shell. It does not start a run.
# Restores the preserved historical observation-law configuration in the
# current safe runner; it does not assert historical binary/trajectory identity.
# Geometry, realization, build, provenance and fresh output paths remain
# mandatory per-case inputs of run_ctpi_fasttrack_case_safe_20260903.sh.
export ARM=M1R
export METHOD=CTPI_G2_M1_M2
export METHOD_FAMILY=ctpi_two_module
export SEED=12
export SENSOR_SEED=12
export TIMEOUT_SEC=240.0
export STEPS_SOURCE_UPDATE=3
export MAX_WARMUP_ITERATIONS=3
export MIN_WARMUP_ITERATIONS=1
export M1R_V41_HISTORICAL_ROLLING_PERSISTENCE=true
