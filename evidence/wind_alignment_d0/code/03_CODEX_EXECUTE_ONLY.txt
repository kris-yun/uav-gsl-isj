Execute the frozen experiment; do not redesign it.

Branch:
`research/wind-alignment-d0-20260926`

Inputs:
- the existing R1 source-blind 87-candidate geometry/maps/manifest;
- House01 config `2,4-1_fast` wind states 0..10;
- `event_state_sequences.csv` from this package.

Rules:
1. Verify this package SHA256SUMS first.
2. Verify 20/20 rows have `unique_state_sequences=1`.
3. Do not read source truth until P0/P1/P2/C1 and all 200 permutation score
   files are frozen and hashed.
4. No GADEN plume generation.
5. No ROS closed loop.
6. No House02/03.
7. No new source, seed, training, learned model or score.
8. P0 parity is mandatory and fail-closed.
9. P1 changes only z: 0 -> 0.30 m under state 0.
10. P2 precomputes 87×11 deterministic PMFS forward maps at z=0.30 m.
11. Use common deterministic random numbers across state arms for each
    candidate.
12. Compute P2-match exactly from the 10 state IDs per event.
13. Compute site-pooled C1.
14. Generate exactly 200 within-site permutations with seed 2026092601.
15. Freeze scores + hashes, commit, then run the truth evaluator.
16. Apply exactly one decision label from `02_EXPERIMENT_SPEC.md`.
17. Stop. Do not start the next stage even if positive.

Return:
- branch
- freeze commit before truth
- final commit
- decision
- P0 parity max error
- Elog/Ebrier truth rank for historical, P1, C1, P2-match
- P2 matched percentile among permutations for both metrics
- top1 center errors
- number of forward maps
- deterministic repeat status
- review ZIP path / bytes / SHA256
