# M4-v3 D0 House02 Development Freeze

Date: 2026-09-23  
Branch: `research/m4-v3-interventional-evolution-propagator`  
Scientific status: **DEVELOPMENT ONLY — House02 is already opened and cannot confirm the paper claim.**

## Purpose

D0 answers one question only:

> Does explicit characteristic transport repair the M4-v2 wind-underresponse without losing source sensitivity?

A D0 pass authorizes freezing M4-v3 and generating new House01/House03 confirmatory data. It is not ADVANCE evidence.

## Frozen inputs

- House02 compact eight-realization GADEN bank already used by C0.5.
- Same sources S1/S2 and physical winds W1/W2.
- Same target concentration snapshots at simulation times:
  `50,75,100,125,150,175,200,225,250,275 s`.
- **New required input:** complete sensor-height W1/W2 wind sequences, iterations 0–10, exported from the same canonical directories.
- Export must pass the frozen iteration-1 SHA-256 anchor:
  - W1: `2fa27ec72a7f05fbaca58b99e4f9390a5c477ee379d7e247e6fde57bbde49846`
  - W2: `54d7bc338ea681611f66004a3230f29feffc495446814607141b0623ef1dd9a8`

No synthetic wind interpolation or speed scaling may substitute for the physical sequence.

## Split

Training target fields only:
- S1-W1 A/B
- S2-W1 A/B
- S1-W2 A/B

Unopened-by-the-training-process evaluation:
- S2-W2 A/B

The D0 evaluator may read S2-W2 only after checkpoints are written and hashed.

## Spatial/time contract

- 2x spatial reduction, matching the v2 development comparison.
- Effective grid spacing after reduction: `0.2 m`.
- Internal transport step: `0.1 s`.
- GADEN wind timing is recreated with float32 current-time semantics and the same update order:
  MoveFilaments first, then wind-index update, then current-time increment.
- Wind iteration period: `1.0 s`.
- Loop: iteration 1 through 10 after the initial iteration 0.

The 0.1 s step is frozen because the observed House02 maximum wind is above 1 m/s; a 1 s characteristic step would cross many 0.2 m cells and make wall handling scientifically indefensible.

**Revision note before execution:** the first D0 freeze named bilinear backward semi-Lagrangian sampling. Before D0 was ever run and before any v3 holdout was opened, the transport was strengthened to a forward conservative characteristic remap plus conservative local closure. This revision was made solely from the physical wall-penetration/mass-balance audit, not from target performance.

## Frozen M4-v3 structure

Implementation:
`research/causal_compositional_plume_world_model_v1/m4_v3_interventional_evolution.py`

Method state (revised before any D0 execution):
1. source is injected as a separate nonnegative forcing field;
2. explicit forward characteristic remap uses local W(t);
3. remap weights are nonnegative and conserve in-domain scalar mass except explicit map outflow;
4. transfers into/crossing obstacles are retained at the source cell, preventing wall penetration;
5. a 5-point local closure is conditioned only on wind and is itself conservative;
6. transport has no source ID, source coordinates, or source-conditioning path;
7. the mapping remains linear in source/concentration state for fixed wind/geometry;
8. learned dependency is local: characteristic reach plus one closure cell;
9. a small source-independent sink is permitted only as an explicit unresolved vertical/outlet-loss term.

The diagnostic `characteristic_scale=0` switch is an ablation only and is never trained as part of the method.

## Training

Two optimization initializations:
- 1729
- 2718

Fixed:
- optimizer: Adam
- learning rate: 0.01
- epochs: 20
- no weight decay
- no validation/early stopping
- objective: free-space MSE of `log1p(predicted concentration-like state)`
  against the same 2x-reduced `log1p(GADEN concentration)` targets used in C0.5.
- A/B plume realizations are two stochastic targets for the same deterministic source/wind response and both contribute equally to training loss.

No S2-W2 metric may alter initialization, epoch count, learning rate, architecture, normalization, or thresholds.

## D0 metrics

For each training initialization and each held-out plume A/B:

- field MSE;
- wind-intervention amplitude ratio
  `rW = ||dW_pred|| / ||dW_true||`;
- wind-intervention spatial cosine;
- centroid-shift magnitude ratio;
- source-intervention amplitude ratio `rS`;
- relative wind/source response `rWS`;
- no-characteristic ablation wind amplitude;
- source superposition audit;
- in-domain transport mass-balance audit;
- obstacle nonpenetration audit.

## Predeclared D0 viability gate

M4-v3 is worth new confirmatory data only if **all four train-seed × plume-seed comparisons** satisfy:

1. `0.5 <= rW <= 1.5`;
2. wind-delta cosine `> 0.5`;
3. predicted/true centroid-shift magnitude ratio `> 0.5`;
4. `rWS > 0.5`;
5. source amplitude ratio `rS > 0.5`;
6. v3 held-out field MSE is below the corresponding frozen monolithic C0.5 MSE;
7. disabling characteristic advection reduces the predicted wind-delta norm by at least 50%;
8. source superposition max absolute error remains below `1e-5`;
9. synthetic in-domain mass-balance error remains below `1e-6` with the explicit sink disabled;
10. obstacle nonpenetration diagnostic has zero material mass across the wall.

If D0 fails, do not generate House01/03 merely to rescue it. Any architecture redesign becomes a new version and must be frozen before another holdout is used.

## Frozen monolithic reference values

S2-W2 field MSE from C0.5:

- monolithic seed1729 / A: 0.442162
- monolithic seed1729 / B: 0.431375
- monolithic seed2718 / A: 0.310335
- monolithic seed2718 / B: 0.297871

These values are reference thresholds only; the monolithic model is not retrained in D0.

## Authorization after D0

If PASS:
- freeze v3 code/checkpoints/thresholds;
- generate fresh House01 and House03 factorial confirmation banks;
- then run C1 mechanism gate and C2 inverse-source ranking.

If FAIL:
- M4-v3 stops at development;
- no G3, ROS/PMFS integration, or closed loop.
