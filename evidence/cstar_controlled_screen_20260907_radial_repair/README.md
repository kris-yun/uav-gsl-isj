# Curvature-preserving M1 diagnostic

Opt-in `--geometry-normalized --coordinate-equivariant --radial-source-score`;
seed/steps/controls unchanged. Physical localization uses the raw candidate
coordinate table. Effective configuration and producer byte hashes are bound
in RUN_START; producer snapshots preserve the implementation.

Verdict: M1_CONTROLLED_SCREEN_NO_GO. Independent RECOMPUTATION checks 2,700
prediction rows and 252 M2 baseline cases. Original flawed-head experiments
remain preserved and separately rescored in the geometry-repair evidence.
