# CTPI-G2 M3 post-H2 idea gate

Date: 2026-09-05

The exact proper H2 mechanism remains frozen as `PROPER_H2_NO_GO`. Two
independent post-H2 M3 candidates were then screened before implementation.

1. The first candidate used a run-permutation null on a symmetric margin
   statistic. The executed coherence trace proved that the permutation
   preserves the margin multiset, hence `R0 = R`, `G = 0`, and every action
   ties. The Phase 3 audit therefore returned `abandon`.
2. The retry candidate used causal-edge monotone transport to reorder source
   evidence. It passed a narrow procedural dry run but triggered the cited
   sub-pattern's hard reject lesson: the proposal imported sequence alignment
   without separating the effect of edge preprocessing from the arbitrary
   alignment rule or comparing the closest same-token alternative. The Phase
   3 audit again returned `abandon`.

Per the user's stop rule, no third M3 candidate is generated and neither idea
is integrated. M3 is frozen as `POST_H2_IDEA_GATE_NO_GO`. The next experiment
is the preregistered House01/02/03 seed-12 `A0/F00/F01` closed-loop screen,
where `F01` activates M1 and M2 while M3 remains disabled.

The idea search used real arXiv and OpenAlex records plus verified host-recall
papers. OpenReview was unavailable and Semantic Scholar was intermittently
rate-limited, so the search is an explicitly degraded collision audit rather
than a complete novelty proof.
