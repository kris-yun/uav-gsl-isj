# 给 GPT 的 M1 审查入口（2026-09-12）

**当前结论：因果 M1 尚未证明跨 House 闭环有效。请审查失败机制和方法本身，不要把下面任何组件 PASS 升格为主创新成功。**

此分支从 `ec391ca56d5ccd501a8f43d64e711fa2d8fb9eda` 保存当前 M1 进展。
本文件覆盖旧检查点中“仍在运行/下一步执行”的过时状态；旧文件按时间保留，不篡改失败历史。

## 1. 必须先知道的事实

- 新版物理/因果核仍主要在 `experiments/ctpi_cstar/m1_causal/`，尚未接进 PMFS 在线源后验更新。
- 原 ROS PMFS 中历史 PHIC/其他旧实现不能当作新版 M1 已接入的证据。
- 新版尚未运行 House123×seed12 的 M1 开关配对闭环；最近几轮过多停留在环境/输入诊断。
- 同代码 H01 两候选预测已完成：普通工作似然在真实 SA 上选 SB；真实 SB 上选 SB。1/2，不是成功。
- V3 使用评估器参考误差半径后两例都弃权。该半径不是部署校准；两例弃权也不是改进收益。
- “每格仅保留最新测风”的对照脚本已编写，但没有执行。VM 目标目录为空；不应写成进行中或有结果。当前不继续扩此风场对照。

## 2. 新版 M1 到底做了什么、还缺什么

已有：固定候选源干预；候选间共享的、按成员/气丝 ID/时间/轴索引的随机数；时序气丝输运和物理 ppm 浓度；持续 FOPDT；地图碰撞/可见性；严格过去时刻的估计风历史读取；条件性 log-evidence 误差界。

缺失：经验证的共享输运不确定性分布/误差校准、完整成员集合下的可靠源排序收益、相对同输入普通贝叶斯的独立贡献、在线 PMFS 后验接口和真实闭环收益。

**审查重点：候选源物理仿真本身不自动构成新的因果理论；鲁棒拒判也不自动改善定位。必须说明因果干预、共享 nuisance 和普通生成式贝叶斯究竟差在哪里。**

## 3. 主要进展与反证

1. 旧 PHIC 的 `aggregate_raw_exposure` 实际是气丝中心占格计数，不是浓度；不能恢复丢失的浓度时序。见 `PMFS_M1_OBSERVATION_CONTRACT_FINDING_20260910.md`。
2. 既有 exact-forward 12/12 两源排名只是条件诊断。修订后输出 UNCALIBRATED，不再将浓度误差和 nats 比较。
3. V3 的扰动界可作条件性确定性界；`Phi(-min_k D_k/2)` 不能当未知输运成员下的部署错误率保证。见 `PMFS_M1_V3_BOUND_CHECKPOINT_20260910.md`。
4. House123 原始 3D 地图哈希/导航高度对齐通过；GMRF 坐标原点和分辨率对齐后，每例 1200 条观测零拒收。
5. 根盘写满导致第一版对齐风场截断，已明确判废。加入写入异常检查，在 tmpfs 重跑六例、复制回本机并校验哈希。有效结果每例 240 s/120 帧；H02 slow 仍有 5 帧未在 20 次迭代内满足收敛标准。
6. H01 SA 在 70.8 s 失败原因为 float32 地图索引与 float64 风索引不一致，已用准确失败坐标复现并修复，保留 v2 失败和 v3 完整结果。
7. 修复后真实 SA：观测峰值 0.414115 ppm、预测峰值 1.276040 ppm；观测事件 229–232 s，预测 229.8–238.4 s；log1p MSE 0.00733753，反而大于 SB 零预测的 0.000806054。
8. 严格“字段时刻早于当前观测”的局部测风检验，六例 GMRF RMSE 为上一条局部测风保持基线的 2.28–2.81 倍。但这不证明保持基线能预测未访问位置，不能直接替换全场。

具体归因：[2026-09-11 记录](PMFS_M1_FAILURE_ATTRIBUTION_20260911.md)。
其中“SB 仍在运行”已过时；现已完成，结果见下方同代码对照。

## 4. 优先阅读的证据与代码

- `evidence/cstar_m1_h01_same_provider_20260911.json`：同实现/同地图/同风输入身份检查、两源排名、条件误差界。
- `evidence/cstar_m1_estimated_transport_h01_screen_240s_SA_trace_v3_20260910.json` 和 `...SB_trace_v3...`：完整时序及代码哈希。
- `evidence/cstar_m1_wind_prequential_20260911.json`：六例严格过去时刻的局部预测检验。
- `evidence/cstar_m1_aligned_wind_complete_audit_20260910.json`：有效风场转存、对齐和数值状态。
- `experiments/ctpi_cstar/screen_m1_estimated_transport_h01.py`：当前端到端诊断入口；不是在线 PMFS。
- `experiments/ctpi_cstar/m1_causal/`：物理、观测、共享随机 key 和条件证据界。
- `evidence/m1_review_upload_20260912/`：大原始文件无损 gzip、逐文件 SHA256、用户提供 V3 理论 zip。

原始文件还原：`python experiments/ctpi_cstar/package_m1_review.py --restore`。
还原时拒绝覆盖不一致文件；与已有相同哈希文件共存。所有摘要 JSON 直接可读，无须先解压。
打包脚本不包含无关旧训练 checkpoint、其他研究路线的未提交编辑，也不修改它们。

## 5. 请 GPT 给出的审查结论

1. 源排序失败主要支持哪种解释：输运预测错配、观测模型错配、候选支持不足，还是创新机制没有区别于普通贝叶斯？哪些仍缺证据？
2. 候选源干预加共享 nuisance 的理论假设在这里是否可识别、可实现？不要把文献启发写成已证明本任务保证。
3. 怎样在不使用真源/真实全场风/未来气体、不按 House 调参的条件下定义和校准成员分布？禁止用风 RMSE 直接充当浓度或 nats 半径。
4. 什么最小实现能真正改善源证据，而不只是多弃权？请给相同 provider 下普通贝叶斯和匹配弃权基线。
5. 下一步应明确指向 M1 在线接口与 House123×seed12 六次配对闭环，还是应判当前主创新假设 NO-GO？不要再无限增加环境诊断或换 seed。

成功标准仍是源证据和真实最终/过程定位误差收益；单 seed 跨 House 开发成功不等于未见数据集泛化成立。
