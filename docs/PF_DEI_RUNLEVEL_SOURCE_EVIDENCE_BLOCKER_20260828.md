# PF-DEI run-level source-evidence execution record

Date: 2026-08-28  
Repository HEAD: `dde7b6e94efb8572838beded6d198bbdb041312c`  
Execution worktree: `codex/pf-dei-runlevel-source-evidence-20260828`

## Preservation checks

The three requested prior work products were left untouched in separate worktrees:

- `a504e0e` — native forward closure;
- `6fbf08c` — sensor-memory audit;
- `33f9a80` — exact sensor deconvolution replay.

No reset, overwrite, source-code change, C++, SBI, Active Probe, truth read, localization-error read, or 60-arm launch was performed.

## Stage 1 results

Both truth-blind reference contracts passed on the VM:

```text
PF_DEI_RUNLEVEL_ENERGY_REFERENCE_SELFTEST PASS
PF_DEI_INVERSE_SENSOR_REFERENCE_SELFTEST PASS
max_abs_physical_recovery_ppm=1.4210854715202004e-14
six_decimal_inverse_error_bound_ppm=6.0138824630974509e-06
```

The frozen deconvolution payload is present for 30 runs and 4,049 blocks. Its hashes are:

```text
recovered_trace_manifest.csv  b838522131d533707e75e1c4d423f8812cd06f41cee0c97e8b413ea0e1cdcf69
block_counterfactual.csv      db4d6554e2e8fab8ada0ec42ffa4138e1ac377852d2679f754973d88c9de82ad
block_counterfactual_summary.json  1e83356e45d6a9ac00f39113605b57efd56c1ce44c0a0d9958d161a2a3bf2ac7
observed_block_manifest.csv   e41937ff436e191210622aee056f02afaf32d2a051a33be2b43bdce38d3ff94c
```

## Stage 2 provenance audit

The required normative payload is `candidate_physical_ppm [S,M,T]` generated by the native GADEN physical-concentration path for a frozen candidate grid and coherent transport members on each of the 30 historical pose/timestamp sequences.

The VM inventory does not contain that payload or a reproducible materializer for it:

- recovered physical observation traces: **30**;
- historical OFF pose traces: **30**;
- candidate physical/forward bank files found by the frozen-name scan: **0**;
- archived context-bank candidate manifests: **150**, with **1,229** distinct adaptive candidate IDs;
- native GADEN source simulation directories: **4 per House**, i.e. 12 total, not a frozen all-candidate grid;
- the existing `candidate_maps/*.f32` and `candidate_support_alignment.csv` are occupancy/hit-map-derived PMFS diagnostics, not physical ppm forward traces and are therefore not admissible substitutes;
- the archived `sensor_trace.csv` contains `true_gas_ppm`, but that forbidden field was not read or used.

A 20-sample read-only probe of the preserved native query binary passed, demonstrating that the binary can sample an existing fixed-source GADEN field. It does not close the missing all-candidate × transport materialization contract.

The A1/A2 canonicalization implementation was also checked on a controlled
synthetic ensemble (not any H01/H02/H03 truth): source-score max difference
`3.469446951953614e-18`, null-score difference `1.734723475976807e-18`, and
identical selected source. This is only an implementation sanity check; it is
not the required historical-candidate parity.

## Stop reason

Stages 3 and 4 were not run because their normative candidate ensemble is missing. Running the energy score against hit maps, `true_gas_ppm`, or one of the 12 fixed-source simulations would be a different experiment and would violate the frozen run-level contract.

Frozen execution status:

```text
STOP_PF_DEI_FORWARD_OPERATOR_UNRESOLVED
RUNLEVEL_INFERENCE_OPERATOR_NOT_QUALIFIED
```

No `PHYSICAL_RUNLEVEL_SOURCE_FORWARD_ACTIONABLE` or `FINITE_TRANSPORT_OR_SOURCE_FORWARD_STILL_INSUFFICIENT` scientific conclusion is claimed. A valid continuation requires either (A) an auditable shared native concentration query plus a frozen candidate grid/transport manifest, or (B) full native GADEN simulations that materialize that payload on the exact 30 trajectories. No result-dependent tuning is permitted.
