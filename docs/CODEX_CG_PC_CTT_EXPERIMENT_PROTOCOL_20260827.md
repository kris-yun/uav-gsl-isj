# SUPERSEDED — CG-PC-CTT V1 protocol

**Status: `INVALID_PROTOCOL`. Do not execute this file.**

The V1 gate failed its own scientific self-test because finite M=8 source-mean noise could be converted into false rank-3 / false absolute source strength under weak and null cases.

The binding replacement on branch `research/cg-pc-ctt-gate-v2` is:

`docs/CODEX_CG_PC_CTT_PROTOCOL_V2_20260827.md`

V2 removes same-member mean-noise squares via a cross-member replicated operator, adds exact member sign-flip null testing, requires explicit transport-member semantics, and blocks H03/H02 evaluation until the real M1 bank is located and provenance-qualified.

No alpha-threshold calibration from V1 is valid in V2.
