# Review-head policy

The exact frozen review head is recorded in the Draft PR metadata/body, not inside this branch, because a commit cannot self-record its own final SHA without changing it.

After the Draft PR is opened, do not add scientific code or result files to this branch. Any later work must use a new branch.
