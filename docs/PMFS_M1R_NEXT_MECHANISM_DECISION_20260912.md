# PMFS M1R next-mechanism decision — 2026-09-12

`OBSERVED_MECHANISM = CUMULATIVE_INTERNAL_BLOCK_COMPOSITE_CONTRASTIVE_LIKELIHOOD`

`REPRODUCIBLE_FAILURE = H03_ENDPOINT_REGRESSION_WITH_SMALL_AUC_IMPROVEMENT`

`MISSING_THEORETICAL_PROPERTY = CONTROLLED_EVIDENCE_RELEASE_UNDER_SOURCE_NONIDENTIFIABILITY`

`ONE_NEXT_MECHANISM = IDENTIFIABILITY_CONTROL`

This is the unique Case-2 decision under the preregistered V4.1 rules.  The
historical artifact audit first returned Case 5, so the one permitted passive
instrumented replication was run.  In new H03, M2 releases an early update
whose nearest true-source rank is 79/85 and whose first-half contrast increment
is negative.  Later contrast becomes strongly net positive and rank can
recover, yet the final source margin remains non-identifiable.  The next study
must therefore concern selective evidence, replication, or abstention that is
explicitly tied to source identifiability—not a generic reduction in update
frequency.

Persistent wrong-sign late evidence is not observed, so transportability is
not selected.  The score rank recovers from 79 to 29, so irreversible
assimilation is not selected.  The posterior remains rank 29/85 with a large
negative margin, so action selection alone is not selected.  No repair is
implemented here.

The cross-domain split precommit also returns
`FAIL_NO_CERTIFIABLY_UNTOUCHED_CONFIRMATION_ASSET`: all twelve currently
catalogued VGR realizations already occur in prior project inventories/screens,
and the project contract forbids adding Houses.  This audit did not read any
new performance, but metadata alone cannot retroactively certify virginity.
