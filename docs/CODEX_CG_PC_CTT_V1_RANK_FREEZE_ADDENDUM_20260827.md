# SUPERSEDED — CG-PC-CTT V1 rank addendum

This file belongs to the invalid V1 protocol and is retained only as provenance.

Do not execute V1 formulas. The binding V2 protocol is:

`docs/CODEX_CG_PC_CTT_PROTOCOL_V2_20260827.md`

V2 still pre-registers the third source-contrast direction (`rank_k=3`), but the statistic is no longer V1 `sigma_3/sigma_1` of a finite-member candidate mean. V2 uses a cross-member replicated operator and exact member sign-flip null testing.
