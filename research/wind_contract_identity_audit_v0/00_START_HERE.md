# Wind-contract identity audit V0

Status: **source-blind mechanism/asset audit only**.

This is not a new localization method, not a G0 experiment, and not permission to
run GADEN or generate a plume.

The immediately preceding two PMFS mechanism hypotheses were falsified:

1. persistent-source marginalization: NULL/ADVERSE;
2. evidence pseudoreplication removal: NULL/ADVERSE.

The next unresolved fact is more basic: the 20 PMFS measurement events contain
wind values that differ strongly from the `/wind_value` spatial field used by
the candidate forward model at source update 1, including the last measurement
block. Before attributing this to temporal wind variation, we must establish
whether the VGR anemometer and `/wind_value` server are reading the same
underlying 11 House01 wind states with the same coordinates and z.

This package performs only that identity audit.
