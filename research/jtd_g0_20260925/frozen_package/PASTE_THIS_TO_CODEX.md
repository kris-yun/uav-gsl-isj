# USER → CODEX HANDOFF MESSAGE

把这个 ZIP 解压到仓库外的临时目录，先阅读 `README_FIRST.md` 和 `CODEX_MASTER_PROMPT.md`。

请严格按包内冻结合同执行 **JTD-G0**。  
不要把任务扩展成闭环、模型创新或新数据生成。

优先从 R0 PASS 的 branch/commit 创建独立 worktree。  
如果找不到 authoritative R0 input，按合同 STOP，不要自己替换数据。

完成后只回报 `CODEX_MASTER_PROMPT.md` 第 7 节要求的 19 项，并把：

`JTD_G0_REVIEW_PACKAGE_20260925.zip`

的路径、字节数和 SHA256 给我。

即使结果 GO，也不要自动跑 ≥143-source confirmation，等我批准。
